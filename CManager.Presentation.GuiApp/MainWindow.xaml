<!--
Microsoft documentation was reviewed to understand WPF window layout,
ContentControl-based navigation, and basic UI composition.

Purpose:
This window serves as the main application shell.
It hosts navigation buttons in the header and dynamically loads
different views into a ContentControl.


Microsoft Docs – WPF Layout Panels
https://learn.microsoft.com/dotnet/desktop/wpf/controls/panels-overview
-->


<!-- Main application window -->
<Window x:Class="CManager.Presentation.GuiApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CManager.Presentation.GuiApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!-- Converter used for visibility bindings (reserved for future UI states) -->
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/> 
    </Window.Resources>
    <Grid>
        <!-- Layout divided into header and content area -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--- Header with navigation -->
        <Border Grid.Row="0" Background="#007ACC" Padding="10">
            <StackPanel Orientation="Horizontal">
                <!-- Navigates to the customer list view -->
                <Button x:Name="CustomerButton" 
                        Content="Customers"
                        Margin="5,0"
                        Padding="15,5"
                        Foreground="White" 
                        Background="#005A9E" 
                        BorderBrush="Transparent"
                        FontWeight="Bold"/>
                <!-- Navigates to the customer list view -->
                <Button x:Name="CreatButton" 
                        Content="Create New Customer"
                        Margin="5,0"
                        Padding="15,5"
                        Foreground="White" 
                        Background="#005A9E" 
                        FontWeight="Bold"
                        Click="CreateButton_Click"/>
            </StackPanel>
        </Border>

        <!-- Main Content Area -->
        <ContentControl Grid.Row="1"
                        x:Name="MainContent"
                        Margin="10"/>
        

    </Grid>
</Window>
